export default class Player{
    
    name:string
    score:number
    ranking:number

    constructor(name:string, score:number, ranking:number){
        this.name = name
        this.score = score
        this.ranking = ranking
    }
}